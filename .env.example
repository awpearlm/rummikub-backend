# J_kube Rummikub Environment Configuration
# Copy this file to .env and fill in your values

# ===== REQUIRED VARIABLES =====

# Database Configuration
# Get this from MongoDB Atlas - see docs/MONGODB_SETUP.md for detailed setup
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/jkube?retryWrites=true&w=majority

# Authentication Secret
# Generate a secure random string (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Environment
NODE_ENV=development

# ===== OPTIONAL VARIABLES =====

# Server Configuration
PORT=8000

# Database Configuration (Optional)
DB_NAME=jkube
MONGODB_URI_TEST=mongodb://localhost:27017/jkube_test

# JWT Configuration (Optional)
JWT_EXPIRES_IN=7d

# Application Configuration (Optional)
SESSION_TIMEOUT=60
GAME_CLEANUP_INTERVAL=30
LOG_LEVEL=info

# Frontend URL (for production)
FRONTEND_URL=https://your-site.netlify.app

# ===== EMAIL CONFIGURATION (OPTIONAL) =====
# Choose ONE of the options below for email functionality

# Option 1: Gmail SMTP (Easiest for personal use)
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an "App Password" in Google Account settings
# 3. Use your Gmail address and the app password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=youremail@gmail.com
EMAIL_PASS=your_16_character_app_password
EMAIL_FROM="J_kube Game" <youremail@gmail.com>

# Option 2: SendGrid (Best for production)
# 1. Sign up at https://sendgrid.com (free tier: 100 emails/day)
# 2. Create an API key in SendGrid dashboard
# 3. Verify your sender email in SendGrid
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# EMAIL_FROM="J_kube Game" <your_verified_email@domain.com>

# Option 3: Mailgun (Also good for production)
# 1. Sign up at https://mailgun.com (free tier: 5,000 emails/month)
# 2. Get your domain and API key from Mailgun dashboard
# MAILGUN_DOMAIN=your_domain.mailgun.org
# MAILGUN_API_KEY=your_mailgun_api_key
# EMAIL_FROM="J_kube Game" <noreply@your_domain.mailgun.org>

# ===== SETUP INSTRUCTIONS =====
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the REQUIRED variables above
# 3. Configure email if you want invitation functionality
# 4. See docs/ENVIRONMENT_SETUP.md for detailed configuration guide
# 5. See docs/MONGODB_SETUP.md for MongoDB Atlas setup

# ===== SECURITY NOTES =====
# - Never commit .env files to version control
# - Use different credentials for development and production
# - Rotate secrets regularly
# - Use strong, random JWT secrets
