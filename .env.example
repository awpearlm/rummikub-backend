# Database
MONGODB_URI=mongodb://localhost:27017/j_kube
MONGODB_URI_TEST=mongodb://localhost:27017/j_kube_test

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Frontend URL
FRONTEND_URL=https://jkube.netlify.app

# Email Configuration - CHOOSE ONE OF THE OPTIONS BELOW:

# Option 1: Gmail SMTP (Easiest for personal use)
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an "App Password" in Google Account settings
# 3. Use your Gmail address and the app password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=youremail@gmail.com
EMAIL_PASS=your_16_character_app_password
EMAIL_FROM="J_kube Game" <youremail@gmail.com>

# Option 2: SendGrid (Best for production)
# 1. Sign up at https://sendgrid.com (free tier: 100 emails/day)
# 2. Create an API key in SendGrid dashboard
# 3. Verify your sender email in SendGrid
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# EMAIL_FROM="J_kube Game" <your_verified_email@domain.com>

# Option 3: Mailgun (Also good for production)
# 1. Sign up at https://mailgun.com (free tier: 5,000 emails/month)
# 2. Get your domain and API key from Mailgun dashboard
# MAILGUN_DOMAIN=your_domain.mailgun.org
# MAILGUN_API_KEY=your_mailgun_api_key
# EMAIL_FROM="J_kube Game" <noreply@your_domain.mailgun.org>

# Environment
NODE_ENV=production
PORT=3000

# IMPORTANT FOR PRODUCTION:
# 1. Set NODE_ENV=production on your server (Render/Heroku/etc.)
# 2. Configure one of the email options above as environment variables
# 3. Without proper email config, invitations will fail in production!
